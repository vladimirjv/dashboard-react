import { ContentPage } from "../../../assets/js/styles";

export const styles = theme => ({
  map: {
    ...ContentPage(theme)
  },
  root: {
    position: "relative",
    flexGrow: 1
  }
});
